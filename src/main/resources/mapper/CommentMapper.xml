<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="javawebdev.petsns.comment.CommentMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into comment (member_id, post_id, content, registered_at)
        values (#{memberId}, #{postId}, #{content}, #{registeredAt})
    </insert>

    <update id="updateCountOfPost">
        update post
        set comment_count = (select comment_count
                             from post
                             where id = #{postId}) + 1
        where id = #{postId};
    </update>

    <select id="findByPostId" resultType="Comment">
        select member_id, post_id, content, registered_at
        from comment
        where post_id = #{postId}
    </select>

    <update id="update">
        update comment
        set content = #{content}
        where id = #{id}
    </update>

    <delete id="delete">
        delete from comment where id = #{id}
    </delete>
</mapper>